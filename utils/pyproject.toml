[project]
name = "interview-vault-utils"
version = "0.1.0"
description = "Utility tooling for the Interview Questions Obsidian vault"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[build-system]
requires = ["setuptools>=67"]
build-backend = "setuptools.build_meta"
